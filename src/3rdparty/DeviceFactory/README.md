# DeviceFactory

Requires:
- OpenCV
- LibRealsense (if enabled, installed automatically by cmake)
- ffmpeg (if enabled)

## Installation
```
git clone https://JVanherck/DeviceFactory.git
cd DeviceFactory
mkdir build
cd build
cmake ..
make
```
Optionally install with `sudo make install`
